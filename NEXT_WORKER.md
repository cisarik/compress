# NEXT_WORKER.md — Worker Pointer

This is the immediate Worker pointer.

Read `NEXT_AGENT.md` for the full Worker handoff.

Read `NEXT_ORCHESTRATOR.md` for strategic context.

## One-Line Focus

`PSMDLR2` is implemented; next is narrow structured-binary evidence gathering around the observed zoneinfo/tzif win.

## Required Report Heading

`### Report for ORCHESTRATOR_CHAT`

Always include `git rev-parse HEAD`.
